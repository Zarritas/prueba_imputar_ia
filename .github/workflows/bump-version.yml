name: PR Version Bump & Merge

on:
  issue_comment:
    types: [created]

jobs:
  bump-and-merge:
    runs-on: ubuntu-latest
    if: ${{ github.event.issue.pull_request && contains(github.event.comment.body, '/mergebot') }}
    permissions:
      contents: write
      pull-requests: write

    steps:
      #
      # Mensaje inicial en el PR
      #
      - name: Enviar mensaje: iniciando proceso
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh api repos/${{ github.repository }}/issues/${{ github.event.issue.number }}/comments \
            -f body="🛠️ Iniciando proceso de merge solicitado con \`/mergebot\`…"

      #
      # Checkout
      #
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Checkout PR
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh pr checkout ${{ github.event.issue.number }}

      #
      # Rebase con main
      #
      - name: Rebase con main
        id: rebase
        continue-on-error: true
        run: |
          git fetch origin main
          git rebase origin/main || echo "REBASE_FAILED=true" >> $GITHUB_OUTPUT

      - name: Error rebase → comentar en PR
        if: ${{ steps.rebase.outputs.REBASE_FAILED == 'true' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh api repos/${{ github.repository }}/issues/${{ github.event.issue.number }}/comments \
            -f body="❌ No se pudo hacer **rebase con main**.  
            Por favor, resuelve los conflictos manualmente."
          exit 1

      #
      # Node y bump de versión
      #
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Determine Bump Type
        id: bump-type
        run: |
          COMMENT="${{ github.event.comment.body }}"
          if [[ "$COMMENT" == *"/mergebot patch"* ]]; then
            echo "TYPE=patch" >> $GITHUB_OUTPUT
          elif [[ "$COMMENT" == *"/mergebot minor"* ]]; then
            echo "TYPE=minor" >> $GITHUB_OUTPUT
          elif [[ "$COMMENT" == *"/mergebot major"* ]] || [[ "$COMMENT" == *"/mergebot mayor"* ]]; then
            echo "TYPE=major" >> $GITHUB_OUTPUT
          else
            echo "TYPE=none" >> $GITHUB_OUTPUT
          fi

      - name: Update Version
        id: update-version
        if: ${{ steps.bump-type.outputs.TYPE != 'none' }}
        env:
          BUMP_TYPE: ${{ steps.bump-type.outputs.TYPE }}
        run: |
          node -e "
            const fs = require('fs');
            const path = 'main/script.user.js';
            if (!fs.existsSync(path)) process.exit(1);

            const content = fs.readFileSync(path, 'utf8');
            const regex = /\/\/ @version\s+(\d+\.\d+\.\d+)/;

            const match = content.match(regex);
            if (!match) process.exit(1);

            let [major, minor, patch] = match[1].split('.').map(Number);
            const type = process.env.BUMP_TYPE;

            if (type === 'major') { major++; minor = 0; patch = 0; }
            if (type === 'minor') { minor++; patch = 0; }
            if (type === 'patch') { patch++; }

            const newVer = `${major}.${minor}.${patch}`;
            const newContent = content.replace(regex, `// @version      ${newVer}`);

            fs.writeFileSync(path, newContent);
            fs.appendFileSync(process.env.GITHUB_OUTPUT, `NEW_VERSION=${newVer}\n`);
          "

      - name: Commit and Push
        if: ${{ steps.bump-type.outputs.TYPE != 'none' }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add main/script.user.js
          git commit -m "chore: bump version to ${{ steps.update-version.outputs.NEW_VERSION }}"
          git push --force-with-lease

      #
      # Merge PR
      #
      - name: Merge PR
        id: merge
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr merge ${{ github.event.issue.number }} --merge --delete-branch || \
          echo "MERGE_FAILED=true" >> $GITHUB_OUTPUT

      - name: Error merge → comentar en PR
        if: ${{ steps.merge.outputs.MERGE_FAILED == 'true' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh api repos/${{ github.repository }}/issues/${{ github.event.issue.number }}/comments \
            -f body="❌ No se pudo completar el **merge**.  
            Puede haber conflictos o restricciones en GitHub."

      - name: Merge correcto → comentar éxito
        if: ${{ steps.merge.outputs.MERGE_FAILED != 'true' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh api repos/${{ github.repository }}/issues/${{ github.event.issue.number }}/comments \
            -f body="✅ **Merge completado con éxito**.  
            Versión actualizada a \`${{ steps.update-version.outputs.NEW_VERSION }}\`."
